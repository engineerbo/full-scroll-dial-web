// ReliableSerialProtocol was a pure delegation wrapper around ProtocolEngine.
// CommandProtocol now implements ProtocolEngineEvents directly, eliminating
// the intermediate layer. This file is intentionally empty.
